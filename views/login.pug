html
	head
		title Chat Login
	body
		form(action = "/", method = "POST")
			div
				lable(for = "name") Name: 
				input(name = "name" placeholder = "Input your name" autocomplete="off" required autofocus)
			br
			button(type = "submit") Enter chat!